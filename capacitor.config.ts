import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nord-pool-price-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
